repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.23.3
    hooks:
      - id: gitleaks
        args: ["detect", "--source", ".", "--report-format", "sarif", "--report-path", "secrets.sarif"]
